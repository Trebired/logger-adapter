[package]
name = "logger-adapter"
version = "0.4.0"
edition = "2021"
license = "MIT"
description = "Rust client for a generic structured logger bridge."

[lib]
name = "logger_adapter"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
